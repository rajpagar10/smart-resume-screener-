# Required for live extraction and matching. Do not commit a real key.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: use a custom SQLite database.
# SMART_RESUME_DATABASE_URL=sqlite:///./smart_resume.db
